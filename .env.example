# Database
DATABASE_URL=postgresql://dcim:dcim_local_password@localhost:5432/dcim

# Auth.js
AUTH_SECRET=generate-with-npx-auth-secret
AUTH_URL=http://localhost:3000

# Initial admin account (used by seed script)
ADMIN_EMAIL=admin@dcim.local
ADMIN_PASSWORD=admin1234

# TimescaleDB (optional — set to true if TimescaleDB extension is installed)
# Required before running: npm run db:timescale or npm run db:setup
TIMESCALEDB_ENABLED=true
