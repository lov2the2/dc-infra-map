apiVersion: apps/v1
kind: Deployment
metadata:
    name: dcim-network-ops
    namespace: {{ include "dcim.namespace" . }}
    labels:
        {{- include "dcim.labels" . | nindent 8 }}
        app.kubernetes.io/component: network-ops
spec:
    replicas: {{ .Values.networkOps.replicas }}
    selector:
        matchLabels:
            {{- include "dcim.networkOps.selectorLabels" . | nindent 12 }}
    strategy:
        type: RollingUpdate
        rollingUpdate:
            maxUnavailable: 0
            maxSurge: 1
    template:
        metadata:
            labels:
                {{- include "dcim.networkOps.selectorLabels" . | nindent 16 }}
            annotations:
                checksum/config: {{ include (print $.Template.BasePath "/network-ops/configmap.yaml") . | sha256sum }}
                checksum/secret: {{ include (print $.Template.BasePath "/network-ops/secret.yaml") . | sha256sum }}
        spec:
            initContainers:
                - name: wait-for-postgres
                  image: busybox:1.36
                  command:
                      - sh
                      - -c
                      - |
                          until nc -z postgres {{ .Values.postgres.service.port }}; do
                            echo "Waiting for postgres..."
                            sleep 2
                          done
                          echo "Postgres is ready"
            containers:
                - name: dcim-network-ops
                  image: "{{ if .Values.global.imageRegistry }}{{ .Values.global.imageRegistry }}/{{ end }}{{ .Values.networkOps.image.repository }}:{{ .Values.networkOps.image.tag }}"
                  imagePullPolicy: {{ .Values.networkOps.image.pullPolicy }}
                  ports:
                      - containerPort: {{ .Values.networkOps.service.port }}
                        name: http
                  envFrom:
                      - configMapRef:
                            name: network-ops-config
                      - secretRef:
                            name: network-ops-secret
                  readinessProbe:
                      tcpSocket:
                          port: {{ .Values.networkOps.service.port }}
                      initialDelaySeconds: 5
                      periodSeconds: 10
                      failureThreshold: 3
                  livenessProbe:
                      tcpSocket:
                          port: {{ .Values.networkOps.service.port }}
                      initialDelaySeconds: 15
                      periodSeconds: 20
                  resources:
                      requests:
                          cpu: {{ .Values.networkOps.resources.requests.cpu | quote }}
                          memory: {{ .Values.networkOps.resources.requests.memory | quote }}
                      limits:
                          cpu: {{ .Values.networkOps.resources.limits.cpu | quote }}
                          memory: {{ .Values.networkOps.resources.limits.memory | quote }}
