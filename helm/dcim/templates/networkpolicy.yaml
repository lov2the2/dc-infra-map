{{- if .Values.networkPolicy.enabled }}
# NetworkPolicy for postgres: only allow ingress from app and go-service pods
apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
    name: {{ include "dcim.fullname" . }}-postgres
    namespace: {{ .Release.Namespace }}
    labels:
        {{- include "dcim.labels" . | nindent 8 }}
spec:
    podSelector:
        matchLabels:
            app.kubernetes.io/component: postgres
    policyTypes:
        - Ingress
    ingress:
        - from:
            - podSelector:
                matchLabels:
                    app.kubernetes.io/instance: {{ .Release.Name }}
---
# NetworkPolicy for power-service: only allow ingress from app (BFF) pod
apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
    name: {{ include "dcim.fullname" . }}-go-service
    namespace: {{ .Release.Namespace }}
    labels:
        {{- include "dcim.labels" . | nindent 8 }}
spec:
    podSelector:
        matchLabels:
            app.kubernetes.io/component: go-service
    policyTypes:
        - Ingress
    ingress:
        - from:
            - podSelector:
                matchLabels:
                    app.kubernetes.io/component: app
---
# NetworkPolicy for core-api: only allow ingress from app (BFF) pod
apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
    name: {{ include "dcim.fullname" . }}-core-api
    namespace: {{ .Release.Namespace }}
    labels:
        {{- include "dcim.labels" . | nindent 8 }}
spec:
    podSelector:
        matchLabels:
            app.kubernetes.io/component: core-api
    policyTypes:
        - Ingress
    ingress:
        - from:
            - podSelector:
                matchLabels:
                    app.kubernetes.io/component: app
---
# NetworkPolicy for network-ops: only allow ingress from app (BFF) pod
apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
    name: {{ include "dcim.fullname" . }}-network-ops
    namespace: {{ .Release.Namespace }}
    labels:
        {{- include "dcim.labels" . | nindent 8 }}
spec:
    podSelector:
        matchLabels:
            app.kubernetes.io/component: network-ops
    policyTypes:
        - Ingress
    ingress:
        - from:
            - podSelector:
                matchLabels:
                    app.kubernetes.io/component: app
{{- end }}
