apiVersion: apps/v1
kind: Deployment
metadata:
    name: dcim-go-service
    namespace: {{ include "dcim.namespace" . }}
    labels:
        {{- include "dcim.labels" . | nindent 8 }}
        app.kubernetes.io/component: go-service
spec:
    replicas: {{ .Values.goService.replicas }}
    selector:
        matchLabels:
            {{- include "dcim.goService.selectorLabels" . | nindent 12 }}
    strategy:
        type: RollingUpdate
        rollingUpdate:
            maxUnavailable: 0
            maxSurge: 1
    template:
        metadata:
            labels:
                {{- include "dcim.goService.selectorLabels" . | nindent 16 }}
        spec:
            initContainers:
                # Wait for postgres to be ready before starting the go service
                - name: wait-for-postgres
                  image: busybox:1.36
                  command:
                      - sh
                      - -c
                      - |
                          until nc -z postgres {{ .Values.postgres.service.port }}; do
                            echo "Waiting for postgres..."
                            sleep 2
                          done
                          echo "Postgres is ready"
            containers:
                - name: dcim-go-service
                  image: "{{ if .Values.global.imageRegistry }}{{ .Values.global.imageRegistry }}/{{ end }}{{ .Values.goService.image.repository }}:{{ .Values.goService.image.tag }}"
                  imagePullPolicy: {{ .Values.goService.image.pullPolicy }}
                  ports:
                      - containerPort: {{ .Values.goService.service.port }}
                        name: http
                  envFrom:
                      - configMapRef:
                            name: go-service-config
                      - secretRef:
                            name: go-service-secret
                  readinessProbe:
                      tcpSocket:
                          port: {{ .Values.goService.service.port }}
                      initialDelaySeconds: 5
                      periodSeconds: 10
                      failureThreshold: 3
                  livenessProbe:
                      tcpSocket:
                          port: {{ .Values.goService.service.port }}
                      initialDelaySeconds: 15
                      periodSeconds: 20
                  resources:
                      requests:
                          cpu: {{ .Values.goService.resources.requests.cpu | quote }}
                          memory: {{ .Values.goService.resources.requests.memory | quote }}
                      limits:
                          cpu: {{ .Values.goService.resources.limits.cpu | quote }}
                          memory: {{ .Values.goService.resources.limits.memory | quote }}
